document.getElementById("register-form").addEventListener("submit", function(event) {
    event.preventDefault(); // Evita el envío del formulario (ajústalo según tu lógica)
    window.location.href = "login.html"; // Redirige a login
});